
function converttousd () {
    let x = document.getElementById ("input").value;
    const y = 0.017;
    const input = parseFloat(x) * parseFloat(y);
    alert ("The conversion is " + input); 
   }

   function converttoyen () {
    let x = document.getElementById ("input").value;
    const y = 2.52;
    const input = parseFloat(x) * parseFloat(y);
    alert ("The conversion is " + input);
   }

   function converttowon () {
    let x = document.getElementById ("input").value;
    const y = 24.38;
    const input = parseFloat(x) * parseFloat(y);
    alert ("The conversion is " + input);

   }


